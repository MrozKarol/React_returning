import React from 'react';

const UsersList = (props) => {
    console.log(props.users)
    return (
        <ul>
            <li>Uzytkownik przykładowy 1</li>
        </ul>
    );
}

export default UsersList;